## popScene

``` sig 
  game.popScene();
```

Removes the current scene off the scene stack and switches back to it.

## Example

``` ts 
    const currSc = game.currentScene();
    game.popScene(); 
```
