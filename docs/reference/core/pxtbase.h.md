## Core Declarations
The `pxtbase.h` file is the declaration of the core properties, function, namespaces, and other core features, including Text, Math, Control, etc.
For example, the method (charCodeAt) has a declaration inside the `pxtbase.h` file.
