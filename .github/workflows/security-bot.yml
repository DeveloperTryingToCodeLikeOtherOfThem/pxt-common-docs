name: AI Bot Investigation
on:
  schedule:
    - cron: '0 */12 * * *' # Every 12 hours
  workflow_dispatch:      # Manual trigger

jobs:
  ai-inspect:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      contents: read
    steps:
      - name: Install Copilot CLI
        run: npm install -g @github/copilot
          
      - name: Copilot Forensic Scan
        id: scanner
        env:
          # In 2026, the CLI prioritizes GH_TOKEN for authentication
          GH_TOKEN: ${{ secrets.COPILOT_TOKEN }}
        run: |
          TARGET="https://enablesmartspirit.blogspot.com/"
          
          # The -p flag must come FIRST.
          # We use --allow-all-tools to give the AI permission to visit the URL.
          # The output is captured directly into the variable.
          
          REPORT=$(copilot -p "Check if $TARGET exists. If it does, find the 'AGC CSR' version. If it is a 404, respond ONLY with '404'." --allow-all-tools)
          
          # Use HEREDOC to handle multi-line AI responses safely
          echo "REPORT_DATA<<EOF" >> $GITHUB_ENV
          echo "$REPORT" >> $GITHUB_ENV
          echo "EOF" >> $GITHUB_ENV

     
      - name: Log Findings to Private Issue
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if [[ "$REPORT_DATA" == *"404"* ]]; then
            gh issue create --title "🎉 Hooray: This domain does not exist." \
              --body "Copilot reports that $TARGET is no longer reachable. Investigation complete." \
              --label "neutralized"
          else
            gh issue create --title "⚠️ Warning: This domain exists and do not click it." \
              --body "### Copilot Forensic Report
              **Target:** enablesmartspirit.blogspot.com
              **Analysis:** $REPORT_DATA
              
              *Automated log created via GitHub Copilot CLI.*" \
              --label "threat-detected"
          fi
